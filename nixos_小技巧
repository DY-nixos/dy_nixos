#noctalia 壁纸打开选择器命令
noctalia-shell ipc call wallpaper toggle
#noctalia 启动器打开命令
noctalia-shell ipc call launcher toggle


# 1. 在 dy 主目录创建配置目录
mkdir -p ~/nixos-config

# 2. 复制所有配置
sudo cp -r /e/nixos/* ~/nixos-config/

# 3. 修改所有权
sudo chown -R dy:users ~/nixos-config

# 4. 在用户目录下重建
cd ~/nixos-config
sudo nixos-rebuild switch --flake .#dynx

#关于关闭clash 断网问题
echo $http_proxy   #检查
echo $https_proxy
unset http_proxy https_proxy all_proxy  #注意这个是bash命令

set -e http_proxy   #fish
set -e https_proxy
set -e all_proxy

set -e http_proxy   # -e 就是 erase（擦除）的意思
set -e https_proxy
set -e all_proxy
set -e HTTP_PROXY
set -e HTTPS_PROXY
set -e ALL_PROXY